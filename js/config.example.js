// Archivo de configuración de ejemplo
// INSTRUCCIONES:
// 1. Copia este archivo como 'config.js' en la misma carpeta (js/)
// 2. Edita config.js con tus valores reales
// 3. NUNCA subas config.js a Git (ya está en .gitignore)

const CONFIG = {
    // URL del backend API de OpenAI
    API_URL: 'https://tu-api-key/chat',

    // (Opcional) Si tu API requiere autenticación con API key
    // API_KEY: 'sk-tu-api-key-aqui'
};

// No modificar esta línea
window.CONFIG = CONFIG;
